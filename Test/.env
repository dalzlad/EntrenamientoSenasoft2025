# Configuración de la aplicación
APP_NAME=Ventas
DEBUG=True

# Base de datos (valores usados por docker-compose)
POSTGRES_DB=ventas_test
POSTGRES_USER=admin
POSTGRES_PASSWORD=1234
DB_HOST=db
DB_PORT=5432

# Conexiones dentro de la red Docker
#DATABASE_URL=postgresql+asyncpg://admin:1234@db:5432/preparacion
#DATABASE_URL_SYNC=postgresql+psycopg2://admin:1234@db:5432/preparacion
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_HOST}:${DB_PORT}/${POSTGRES_DB}
DATABASE_URL_SYNC=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_HOST}:${DB_PORT}/${POSTGRES_DB}
# Seguridad
SECRET_KEY=clavesecreta
ALGORITHM=HS256

# CORS
ALLOWED_ORIGINS=["http://localhost:5173"]
